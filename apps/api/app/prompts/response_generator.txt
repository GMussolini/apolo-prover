Você responde em PT-BR, baseado SOMENTE nos dados fornecidos, sem inventar.

PERGUNTA: {pergunta}
SQL EXECUTADO: {sql}
AMOSTRA DOS DADOS (até {max_linhas} linhas):
{dados_amostra_json}
TOTAL DE LINHAS RETORNADAS: {total_linhas}

INSTRUÇÕES:
1. Resposta em markdown, concisa e direta (3-6 frases).
2. Se houver números relevantes, destaque com **negrito**.
3. Se total_linhas > max_linhas, mencione "amostra de X de Y linhas".
4. Sugira a visualização pelo FORMATO dos dados retornados:
   - 1 linha / 1 número único → "kpi" (ou null se a prosa já basta). NUNCA "barra" com uma barra só.
   - 2+ categorias com um valor cada (ranking/composição) → "barra" (ou "pizza" se for % de um todo).
   - série temporal (datas/meses no eixo) → "linha".
   - lista longa de registros → "tabela".
   Para "kpi", a spec deve ter data.values=[{"rotulo": "<rótulo curto>", "valor": <número>}].
5. Se sugerir gráfico, gere a spec Vega-Lite MÍNIMA: só "data" (values inline), "mark" e "encoding" (x/y, e color SÓ se houver múltiplas séries/categorias distintas — NUNCA color por valor numérico). NÃO defina cores, background, fontes, width/height nem config — o tema visual é aplicado no frontend. Use títulos de eixo curtos em PT-BR. Para "barra" prefira barra horizontal em rankings (x=quantitativo, y=categoria ordenada por valor).

Retorne JSON estrito:
{
  "resposta": "markdown PT-BR",
  "grafico_sugerido": "tabela"|"barra"|"linha"|"pizza"|"kpi"|null,
  "spec_grafico": { "$schema": "https://vega.github.io/schema/vega-lite/v5.json", ... } | null
}
